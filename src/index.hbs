---
layout: layout.hbs
pageId: home
pageTitle: "Lisa Marleen Portfolio 2022"
---

<section id="intro" class="intro">
    <h1 class="intro-title">{{{home.title}}}</h1>
    <nav class="intro-nav">
        <ul>
            {{#each common.navigation}}
                <li>
                    <a href={{this.link}}>{{this.label}}</a>
                </li>
            {{/each}}
        </ul>
    </nav>
</section>
<section class="short-bio">
    {{home.shortBioTitle}}
</section>
